name: new_dapoer_plan_project_fresh
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.1
  image: ^4.0.15
  image_picker: ^1.1.2
  http: ^1.4.0
  cupertino_icons: ^1.0.8
  tflite_flutter: ^0.11.0
  # tflite_flutter_helper: ^0.3.1 
  logger: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/model.tflite
    - assets/model2.tflite
    - assets/model3.tflite
    - assets/labels.txt